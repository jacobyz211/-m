{
  "name": "soundcloud-deezer-eclipse",
  "version": "1.0.0",
  "description": "Eclipse SoundCloud + Deezer (SNIP-only) addon for Cloudflare Workers",
  "private": true,
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy"
  },
  "dependencies": {
    "hono": "^4.4.0"
  },
  "devDependencies": {
    "wrangler": "^3.57.0"
  }
}
